"description_1" : "<span class=\"translated\">With a short or long button press the siren will be armed for the set time. If a switch on delay is set, the siren will be armed after the time has expired. A short button press activates the absence mode (all sensors are active), a long button press activates the presence mode (only sensors that monitor the shell are active).</span><br/><br/></span><br/><br/>",
"description_2" : "<span class=\"translated\">With a short or long button press the siren is disarmed and the alarm output is stopped.</span><br/><br/></span><br/><br/>",
"description_3" : "<span class=\"translated\">With a short or long button press the siren will be armed or disarmed for the set time, depending on the current status (toggle function). If a switch on delay is set, the siren will be armed after the time has expired. For activating the alarm, a short button press activates the absence mode (all sensors are active), a long button press activates the presence mode (only sensors that monitor the shell are active).</span><br/><br/></span><br/><br/>",
